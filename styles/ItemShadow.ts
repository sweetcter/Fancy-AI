import { StyleSheet } from "react-native";
const itemShadow = StyleSheet.create({
  shadow: {
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 16,
    },
    shadowOpacity: 0.3,
    shadowRadius: 12,
    elevation: 8,
    // backgroundColor: "white",
  },
});

export default itemShadow;
